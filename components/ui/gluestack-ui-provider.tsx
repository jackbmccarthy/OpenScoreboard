// GlueStack UI Provider - simplified for Next.js
// In v3, the provider setup is minimal for web

export function GluestackUIProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
