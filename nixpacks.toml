[build]
  build_command = "npm install && npm run build"

[static]
  public_dir = "dist"

[start]
  # Use http-server for proper static file serving
  # -c-1 = disable caching for debugging  
  cmd = "npx --yes http-server dist -p $PORT -c-1"
