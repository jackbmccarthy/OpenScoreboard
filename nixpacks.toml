[build]
  build_command = "npm run build"

[static]
  public_dir = "dist"

[start]
  # Use --single to enable SPA fallback (serves index.html for unknown routes)
  cmd = "npx --yes serve dist --single -l $PORT"
