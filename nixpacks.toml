[build]
  build_command = "npm install && npm run build"

[static]
  public_dir = "dist"

[start]
  # Serve static files from dist
  # Don't use -s (single mode) as it redirects /assets/ too
  cmd = "npx --yes http-server dist -p $PORT -c-1"
